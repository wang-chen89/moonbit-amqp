# 小缓冲消息发送修复 · 2026-09-21

已在内存中的不超过 64 KiB 的消息使用原有 MoonBit 完整发布编码器，一次写出方法、内容头和正文帧，避免走异步流迭代造成的分次写出。更大的缓冲和调用方流继续使用已有背压路径。确认序号只在完整校验后分配；调用方缓冲仍立即复制。

客户端 18 组、确认 21 组、流式 23 组、自定义传输 22 组、恢复 25 组、关闭期限 17 组通过。新增边界覆盖 0/1/4088/4089/65536/65537 字节、4096 字节协商帧、原始内容、调用方修改和确认顺序。未修改 MoonBit 核心及生成引擎，因此未重复核心后端套件。

同机 Go、修改前和修改后各独立进程、轮换顺序执行三种负载各三轮。消息正文、数量、顺序和发布确认全部核验。小消息 128 条的中位发布时间由约 5623 毫秒降至约 90 毫秒；原有异常延迟消除。

| 负载 | 发布耗时/修改前 | 发布耗时/Go | 接收耗时/Go |
|---|---:|---:|---:|
| tcp-128-1-get | 0.016× | 2.07× | 1.63× |
| tcp-65536-32-get | 0.805× | 3.62× | 8.10× |
| tls-4096-32-consume | 0.249× | 2.95× | 11.11× |

接收路径未改，大消息接收仍比 Go 慢约 8–11 倍。该本机有限负载不证明生产性能全面相等；不继续围绕同一项目扩展性能任务。

原始结果见 evidence/buffered-performance.json；当前源码指纹见 evidence/buffered-upgrade.json。上一轮合集保留原样，不能当成本补丁的归档。
