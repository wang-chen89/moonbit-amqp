import {
  "localreview/amqp" @lib,
}

supported_targets = "js"

pkgtype(kind: "executable")

options(
  link: {
    "js": { "exports": [ "run", "inspect_wire", "schemas" ], "format": "esm" },
  },
)
