# 复核说明 · 2026-09-22

当前建议：**条件复审**。

## 针对三类反馈的处理

1. **仓库不能克隆**：本项目地址为 https://github.com/wang-chen89/moonbit-amqp。2026-09-22 匿名新克隆成功；默认分支 `main`，核验公开提交 `010645e553e32ffcc9b419d4363db1754d367a02`。本轮源码修订仅在本地，尚未推送；此记录不证明当时报名表中的地址正确，也不证明新修订已上线。
2. **生态已有实现**：承认 DDD12345-D/moon-amqp 已有编解码与 JS broker 会话，MoonMQ 已有 broker 核心。对照固定提交的文档，前者明确不提供 TLS、心跳和 confirms。本项目重点是有界流式发送、确认状态、恢复拓扑和旧 tag 拒绝，不能把所有差异都称作 MoonBit 原生网络实现。未证明对方完全不能通过二次开发实现这些任务。 不再用同名搜索未命中概括整个技术类别为空白。
3. **选题简单、过窄或用途不足**：需要从 MoonBit 维护协议状态、并由 JS 宿主处理可靠发布时可评估；如果项目只需现成 Node 客户端，没有必要为“原生”再造一套。 同类实现重叠；尚需由接入方确认恢复/大消息场景与宿主分工足以构成独立贡献。

## 本地实际变化

- 将入口改成当前版本、模块名、完整自身 URL、具体输入/操作/输出；旧记录单独存档。
- 增加可运行本地任务及来源性质标注，不把离线样例冒充第三方服务器、客户或生产验证。
- 前一轮工程验证独立编码的本机 TCP peer 复现断连、恢复、旧标签、12 MiB 数据散列、队列背压及部分发布失败；23 组流式检查通过。不是前一轮工程验证新跑的 RabbitMQ 集群验收。

## 仍不能声称已解决

不是新协议，也不是 RabbitMQ 完整客户端等价物。断连时未确认消息可能已到达 broker，不能宣称 exactly-once；部分独立测试使用手写 TCP peer，其结果与真实 RabbitMQ 的历史报告分开。

本次修订不能保证初审通过。对于已驳回的选题，只修复链接或材料不等于解除价值异议；没有具体需求支撑时，应暂缓复申或评估换题。项目作者、参赛人及账号归属仍由团队按真实关系填写，本文不代填身份或生成使用方证明。
