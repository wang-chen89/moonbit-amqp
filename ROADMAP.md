# 后续完善

0.8 已补自动删除关联清理、跨通道/未完成操作防护与真实原库对照；0.7 已增加连接内凭证更新与真实 OAuth 刷新/权限/过期验证；0.6 已增加三种认证、自定义初始响应、mTLS/重连验证；0.5 已增加可选连接/通道/拓扑恢复；0.4 已完成基本连接/认证/通道状态机、Node TCP/TLS、消息发布/消费/确认/拒绝/事务，并有独立 RabbitMQ 4.0.5 实测。当前仍不能认定追平完整成熟客户端。

1. 完整恢复/上游 API 语义、外部拓扑变化及更复杂并发恢复、认证失败策略和长时间断网/集群切换；保持不隐式重发的边界。
2. 其它客户端扩展、更多身份提供器/认证失败策略验证；扩大字段表方言与域语义覆盖。令牌获取和刷新调度由应用负责，和固定原库一致。SASLprep 不自动执行，与固定原库一致。多轮挑战超出当前参考原库能力，尚未实现。
3. 流式大正文、可等待的背压、消费者负载控制和长期故障/资源恢复测试。
4. 多版本 RabbitMQ、多操作系统网络宿主及与成熟库的代表性性能比较。
5. 最终验收时让源码、文档、证据、ZIP/bundle 指向同一提交；当前所有工作保存在本地。
