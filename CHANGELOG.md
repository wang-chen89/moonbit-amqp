## 0.8.0 · 2026-09-19

- 自动删除队列在最后一个登记消费者取消后从恢复状态移除，并逐级清理失去最后出站绑定的自动删除交换机。队列删除、解绑和交换机删除共用迭代清理，支持循环图。
- 跨通道消费者和挂起 consume/bind 纳入判断；显式消费者通道关闭、服务端取消与队列别名清理得到验证。意外连接/通道丢失仍恢复原有意图。
- 新增 13 故障组、2 真实 broker 生命周期组和 10 个 Go/Node 真实重连对照：9 一致，1 个空解绑从未绑定交换机的登记差异明确保留。被动声明/404 与确认消息证明真实恢复行为。
- 核心保持 JS/Wasm-GC 各 109 项；认证、凭据更新及原有恢复回归通过。完整 API、生产负载、流式/背压和性能追平仍未完成。

## 0.7.0 · 2026-09-19

- 接入 connection.update-secret：MoonBit 跟踪唯一挂起更新并拒绝意外确认，Node 原连接/恢复连接增加 updateSecret，独立于通道 RPC 和流控。
- 超时、关闭、取消和 broker 拒绝会结束更新；断线后不重放，不自动改变原认证候选，应用提供器可返回新令牌。
- JS/Wasm-GC 各 109 项；10 组更新故障、10 组真实 RS256 OAuth/TLS/刷新/权限/恢复，原版 Go 6 报文和 6 broker 结果对照通过。既有传输/恢复/认证与原库对照继续通过。
- 记录并修正测试假设：RabbitMQ 4.0.5 先确认过期替代令牌，后以 403 拒绝业务；Go 原库相同。保留初次失败观察，未把该情形宣称为有效令牌更新。
- 完整恢复/客户端扩展、流式/背压及生产/多平台验证继续；仅本地提交与同提交归档。

## 0.6.0 · 2026-09-19

- MoonBit 增加 Authentication 与候选选择、PLAIN/AMQPLAIN/EXTERNAL、原始字节自定义响应、延迟提供器接口及 locale；公开认证事件和已选机制。
- Node 增加可取消的异步初始响应提供器、重连候选快照/重新协商；CLI 支持 SASL 优先序和客户端证书。认证 JSON 错误不回显凭证。
- 核心 JS/Wasm-GC 各 104 项；30 个固定原库认证响应、9 个独立协商对照，12 组认证故障、10 组真实 RabbitMQ 认证/双向 TLS/重连/CLI 通过；18+25 原有故障、22+6 原有 broker 流程继续通过。
- 原库不自动 SASLprep；本版保持原始 Unicode。PLAIN NUL 拒绝、候选/响应限长、locale 预校验为明确本地边界。AMQPLAIN 对照仅忽略字段顺序。
- 保留 update-secret/OAuth 凭证更新、全部恢复边界、流式/背压和生产/跨平台性能差距；仅本地提交及同提交 ZIP/bundle。

## 0.5.0 · 2026-09-19

- 增加可选连接/通道恢复与有界重试，稳定逻辑通道和队列别名；恢复 QoS、确认/事务模式、交换机/队列/两种绑定/消费者。
- 增加 exchange bind/unbind 宿主 API，拓扑三种恢复模式和实体错误回调；显式删除/取消移除登记，不重发未确认发布或未知结果 RPC。
- 拒绝旧投递标签，限制恢复阶段缓存；取消重试、关闭竞态、连接恢复取代通道恢复、监听器异常隔离。
- 核心 JS/Wasm-GC 各 91 项；18 组网络故障、25 组恢复故障、22 项原有 RabbitMQ 和 6 组恢复集成；固定原版 Go 单个重复断线场景一致。
- 当前源码/引擎/证据同提交本地 ZIP/bundle；性能仅本机样例。完整恢复边界、更多认证、流式大消息及生产/跨平台验证仍缺。

## 0.4.0 · 2026-09-16

- 新增 MoonBit 客户端会话：PLAIN、连接调参、vhost、通道生命周期、消息组装、流控与关闭竞态处理。
- 新增 Node TCP/TLS 客户端、心跳、RPC/确认超时、取消和有界缓冲；多通道隔离、队列/交换机、发布/消费、确认/拒绝/重投、事务。
- 新增 roundtrip/publish/get CLI，默认禁止明文密码连接，TLS 验证证书与主机名。
- 91 项核心测试在 JS/Wasm-GC 通过；18 组网络故障专项、22 项独立 RabbitMQ 4.0.5 流程通过，含单机确认计时样例。
- 未完成自动恢复、更多认证、流式大消息与生产规模验证。旧 ZIP/bundle 未更新。

## 0.3.0

- 64 个规格方法的结构化参数、bit 打包和通道验证。
- RabbitMQ 字段表、14 个 Basic 属性、完整内容帧构造和严格组装模式。
- 元数据缓冲上限、短字符串 UTF-8、嵌套/节点限制与错误处理。
- Pika 1.3.2 的 220 个独立字节向量，80 项 JS 测试通过。
- 二进制/十六进制文件审查、方法表查询及浏览器 inspect/strict 模式；7 个 CLI 场景通过。
- 带来源与 BSD 许可的官方协议表。尚无真实 RabbitMQ 联网验证。

# 0.2.0 本地审查更新

增加 Basic.Publish/Return/Deliver/Get-Ok 内容帧组装；支持多通道交错和零正文，检查顺序、声明长度、64 个在途通道和全局正文缓存上限。方法参数及属性仍是 opaque payload，真实 RabbitMQ 互通未执行。
