## 0.4.0 · 2026-09-16

- 新增 MoonBit 客户端会话：PLAIN、连接调参、vhost、通道生命周期、消息组装、流控与关闭竞态处理。
- 新增 Node TCP/TLS 客户端、心跳、RPC/确认超时、取消和有界缓冲；多通道隔离、队列/交换机、发布/消费、确认/拒绝/重投、事务。
- 新增 roundtrip/publish/get CLI，默认禁止明文密码连接，TLS 验证证书与主机名。
- 91 项核心测试在 JS/Wasm-GC 通过；18 组网络故障专项、22 项独立 RabbitMQ 4.0.5 流程通过，含单机确认计时样例。
- 未完成自动恢复、更多认证、流式大消息与生产规模验证。旧 ZIP/bundle 未更新。

## 0.3.0

- 64 个规格方法的结构化参数、bit 打包和通道验证。
- RabbitMQ 字段表、14 个 Basic 属性、完整内容帧构造和严格组装模式。
- 元数据缓冲上限、短字符串 UTF-8、嵌套/节点限制与错误处理。
- Pika 1.3.2 的 220 个独立字节向量，80 项 JS 测试通过。
- 二进制/十六进制文件审查、方法表查询及浏览器 inspect/strict 模式；7 个 CLI 场景通过。
- 带来源与 BSD 许可的官方协议表。尚无真实 RabbitMQ 联网验证。

# 0.2.0 本地审查更新

增加 Basic.Publish/Return/Deliver/Get-Ok 内容帧组装；支持多通道交错和零正文，检查顺序、声明长度、64 个在途通道和全局正文缓存上限。方法参数及属性仍是 opaque payload，真实 RabbitMQ 互通未执行。
